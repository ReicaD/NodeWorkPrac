//creating variables so we can use node to run them.
const cars = ['toyota','Combo','Yuri'];

console.log(cars);
// calling all the vehicles in sequence below using an arrow function
 const vehicles = (all)=>{
console.log(`great deal today,${all}`)
 }